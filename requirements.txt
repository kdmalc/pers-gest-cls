# Runtime + dev dependencies for pers-gest-cls.
# The canonical cluster environment is fl_torch_NOTS.yaml (conda, python 3.10,
# torch 2.4.0+cu12.1). This file exists so the DATA-FREE test suite in tests/
# can be installed and run anywhere, including a laptop with no GPU and no
# cluster data. Versions are floors, not pins -- fl_torch_NOTS.yaml is the pin.

torch>=2.4
numpy>=1.26
scipy>=1.11
pandas>=2.0
optuna>=4.0

# Analysis / figures
pingouin>=0.5
matplotlib>=3.7
seaborn>=0.13
scikit-learn>=1.3

# Dev only
pytest>=7.4
